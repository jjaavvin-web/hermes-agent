# MASTER-PLAN CHAPTER 1 — SECURITY + BILLING

Scope: consolidation of 2026-06-10 security/billing outcomes from `20260610-foundation-synthesis`, `20260610-prereview`, `20260610-session-pins`, `20260610-discord-context-mine`, and `20260610-merge-regression-sweep`.

Doctrine filter: **simple-first, delete-over-build, no new automation loops.** This chapter is an execution list, not another audit.

Current commit state was verified read-only with `git -C /home/josep/.local/share/hermes-agent log --oneline -8`: `69b05e45b`, `2cdd3bd01`, `a339a4484`, and `b779a2f1d` are present at the top of the live checkout.

## (A) DONE table

| Done item | Evidence pointer | Why it matters |
|---|---|---|
| `run_claude_oneshot()` now delivers `system_prompt` through the handoff file instead of silently dropping it. | Commit `69b05e45b` in current log; `git show --stat`: 2 files changed, 53 insertions / 1 deletion; prereview `FIXES-REVIEW.md` A1 = SHIP-WITH-NITS. | Protects the June-15 Max/OAuth reroute path from silently running without the schema/registry guardrails. |
| Title generation shutdown SIGABRT race is bounded/drained. | Commit `2cdd3bd01` in current log; `git show --stat`: 2 files changed, 154 insertions / 3 deletions; prereview A2 = SHIP-WITH-NITS. | Stabilizes short-lived CLI/one-shot runs used by the security/billing remediation lanes. |
| Claude CLI handoff tempdirs are cleaned after turns, with opt-in retention only. | Commit `a339a4484` in current log; `git show --stat`: 2 files changed, 59 insertions / 4 deletions; commit body explicitly fixes prereview concern: prompt residue under `/tmp`. | Closes the local prompt-residue leak created by the Max/OAuth handoff route; especially important for lecture transcripts, extraction registries, and system prompts. |
| Codex stale-timeout silent recovery clobbered by the 0.16.0 merge was restored. | Commit `b779a2f1d` in current log; `git show --stat`: 2 files changed, 166 insertions / 1 deletion; merge sweep P1 clobber `3251841f5` marked absent in `20260610-merge-regression-sweep/REPORT.md`. | Restores the no-visible-abort recovery path for Codex subscription calls; prevents false failures from being misread as provider/billing instability. |
| Current auth surface was verified as OAuth-only plus one OpenRouter prepaid key; no gateway-reachable OpenAI/Anthropic/Google API keys found. | `20260610-foundation-synthesis/REPORT.md` C13 and refuted claims §2. | Establishes the baseline: after deleting fallback paths and dead native pins, the box should have no metered API route. |
| Discord session-pin audit found no pinned rows using dead/retired native Anthropic, `claude-code-*`, or OAuth-spoof route classes. | `20260610-session-pins/AUDIT.md`: 71 pins joined; 0 dead/retired route refs; 66 explicit route pins; 2 startup auto-continue rows. | Removes one suspected restart-billing vector, while leaving concrete cleanup work for stale pins and auto-continue rows. |
| Windows firewall mitigation for Supabase LAN exposure was discovered and recorded. | `20260610-foundation-synthesis/REPORT.md` coverage gap 2 and D4 correction. | Downgrades the shape of the Supabase bind risk: the real residual is credentials/JWT plus observability ports, not the already-blocked Supabase port set. |
| Backup secret-exclusion fix exists as PR #70, already tested, but not merged. | `20260610-discord-context-mine/REPORT.md` novel finding 3: PR #70 open, commit `0b4c32927`, 105 tests. | Converts C9 from “design a fix” to “review/merge/verify + shred leaked snapshots”; no new implementation loop needed. |
| Anthropic endpoint substring check and specific credential lease bypass have a reviewed fix candidate. | `20260610-prereview/FIXES-REVIEW.md` target `e3207e5ab` = SHIP-WITH-NITS. | The dangerous shape is known and already patched in a lane; remaining work is harvest/merge plus edge tests, not rediscovery. |

## (B) REMAINING table

| Seq | Open item | OWNER | Packet sketch / execution note | Why it matters |
|---:|---|---|---|---|
| 1 | Delete the OpenRouter `fallback_providers` block and set an OpenRouter hard spend cap / split key for Honcho vs gateway. | josep-gated | Run from owner terminal/account: remove fallback block from live config, restart only in the approved config window, then set the OpenRouter account hard cap. Also answer whether the Honcho key is prepaid/ZDR; corpus says spend limit was `None`. | This is the largest direct billing risk: one uncapped key backs both Honcho and the broken gateway fallback chain. |
| 2 | Finish the 3 June-15 bare `claude -p` reroutes correctly: `claude_summary.py`, `claude_oauth_extractor.py`, `opus_extractor.py`. | LANE | Packet: start from `20260608T180839Z-48h-codex-burn-goal/lanes/lane1-claude-p-migration-unapplied.patch`; route through `run_claude_oneshot`, pin caller tool/permission scope, preserve local schema validation, and add a batch-level parse-failure circuit breaker. Verify `grep` over all 3 consumers = 0 bare `claude -p`; smoke with `env -u CLAUDECODE`; do not grant new tools. | C1 becomes P0 on 06-15; the prereview showed the first reroute attempt regressed schema enforcement and could double-call on parse failures. |
| 3 | Harvest/merge the credential-guard fix candidate (`e3207e5ab`) and add edge tests. | LANE | Packet: port hostname parsing and `acquire_lease(credential_id=...)` guard into the live target; add tests for userinfo, ports, trailing dot normalization, scheme-less input, IPv6/query tricks, and exhausted specific IDs. Verify no production caller depends on bypass behavior; focused auth tests only. | Protects the OAuth-only invariant from future direct Anthropic endpoint spoofing or forced leasing of exhausted/wrong-auth credentials. |
| 4 | Review/merge PR #70 for `backup.py` secret exclusion, then shred leaked snapshot secrets. | josep-gated | Owner-gated split: review/merge PR #70 in the approved source gate; after merge, shred `state-snapshots/20260609-031933-pre-update/.env` and `auth.json` from owner terminal. Re-verify next `hermes update` excludes `.env`, `auth.json`, `relay.secret`, `*.key`, `*.pem`. | Current snapshots contain plaintext secrets and Windows `\\wsl$` makes Linux `0600` insufficient as a containment story. |
| 5 | Restore Fable profile isolation: remove profile `.env`/`auth.json` symlinks to the main credential store and restore the backup auth file. | josep-gated | Execute the exact gated block from `FOUNDATION-PLAN.md` C3; verify profile reads global fallback only where code permits read-only fallback, not by symlinked writable credentials. | Shared auth symlinks create credential write-race and collapse the profile isolation boundary. |
| 6 | Archive/repoint native Anthropic profile set and stale creds: `claude-code-{queen,coder,auditor}` plus broken h2simple/h2research/h2dispatch/cheapgrunt routing exposure. | josep-gated | Config/profile edit under owner gate: archive dead native profiles or repoint only where explicitly wanted; delete stale profile-local credential copies; prune these names from triage prompts. | Dead native Anthropic routes become billed or broken after June-15 and keep attracting classifier/board work. |
| 7 | Repoint `auxiliary.approval` and `triage.llm_classifier` off dead Anthropic/native pins to `openai-codex/gpt-5.5` or `auto`. | josep-gated | Fold into the same config edit/restart as item 1; verify smart approval no longer logs dead model resolution and triage prompt no longer routes to archived profiles. | Avoids silent heuristic-only approvals and regex-only triage while preventing native Anthropic fallback. |
| 8 | Smoke Anthropic OAuth pool once before any h2reviewer/h2governor dispatch. | josep-gated | Run exactly one owner-terminal smoke: `hermes -p h2reviewer ... 'reply OK'`; treat last_status as advisory because exhaustion TTL expired. Do not dispatch h2 lanes until the smoke passes. | `last_status=exhausted` is not durable truth; the only safe gate is one real Max/OAuth call, not assumptions. |
| 9 | Rotate Supabase `POSTGRES_PASSWORD` and demo `JWT_SECRET`, after the orphan Honcho dump and in the same watched window as DSN-home work. | josep-gated | Owner DB/config window only: dump orphan first, rotate PG/JWT, update consumers, test direct PG + Kong/service-role behavior, keep rollback notes. | Anyone reaching Kong with the demo JWT secret can mint service-role access; default DB creds are not a foundation. |
| 10 | Block or unpublish LAN-reachable Grafana/Prometheus/OTel ports: 3000, 9090, 4317, 4318, 8889. | josep-gated | Elevated Windows PowerShell: extend the existing `GOATTRADE Local Supabase - Block External Inbound` rule or recreate containers on loopback-only binds; verify with one LAN probe. | Supabase ports are already firewall-blocked; observability ports are the actual LAN-open residual. |
| 11 | Disable Tailscale logon re-armers until posture is decided: `Ensure-Tailscale-Online.ps1` startup link and `HermesMobileDashboardBridge` scheduled task. | josep-gated | Owner Windows session: disable/remove the startup link and schtask; record the posture decision before next login attempt. | “Tailscale logged out” is not a control if boot/login automation tries to re-open exposure. |
| 12 | Decide unattended-reboot posture: accept/document manual login dependency or configure Windows auto-logon + lock. | CLAUDE | Judgment work: write the tradeoff note and recommended option; no new watchdog loop. Josep executes if he accepts auto-logon risk. | A Windows Update reboot can park Docker/WSL/gateway dead at login while the WSL watchdog is also dead. |
| 13 | Scope `mvms-writer` away from default subagent inheritance. | josep-gated | Config edit only: disable `delegation.inherit_mcp_toolsets` for writer-capable MCPs or profile-scope `mvms-writer`; restart under approved config gate. | Audit workers should not be able to contaminate high-integrity memory by default. |
| 14 | Tighten gateway command rails: prune command allowlist, set `destructive_slash_confirm=true`, review `gateway.strict` and `trust_recent_files`. | josep-gated | Fold into item 1/7 config window; verify effective config after restart and do not add new slash/automation machinery. | A wide command surface plus no destructive confirmation is a security boundary failure, not UX convenience. |
| 15 | Clean Discord session pins and startup auto-continue rows. | CLAUDE | Use `20260610-session-pins/AUDIT.md` and `20260610-discord-context-mine/REPORT.md` §5: first clear the 2 `resume_pending` rows after snapshot; then strip/archive only junk/stale pins after active-lane spot-check. Keep OPUSHANDS/KANBAN control threads. | Prevents stale threads from ambient follow-up handling or startup synthesized turns; session-pin audit ruled out dead billing pins but found real clutter. |
| 16 | Rewrite routing doctrine docs to the settled two-lane model and include the June-22 Fable 5 billing boundary. | CLAUDE | Judgment/writing pass: OpenAI-Codex subscription default; Claude CLI subprocess premium Max/OAuth lanes; explicit forbidden native Anthropic/API-key paths; Fable 5 free-until-06-22 and ~5% Opus fallback caveat. | Stale docs keep recreating billing mistakes and profile sprawl. |
| 17 | Add app-state backup coverage for `kanban.db`, `config.yaml`, `cron/jobs.json`, and `~/.claude` memory, riding the existing backup timer. | LANE | Packet: add one tar/gzip stanza to the existing backup script; verify archive contents and `gzip -t`; do not create a new timer; owner only arms/reloads if needed. Include OnFailure coverage if already using the same drop-in pattern. | Current backups protect DB schemas but miss PM state, config, cron, and file-memory hard gates. |
| 18 | Add Honcho live DB + orphan corpus backup stanza, with `gzip -t` verification and OnFailure coverage. | LANE | Packet: extend `mvms-backup.sh` to dump both `honcho-database-1` and the orphaned Supabase public corpus; write logs/state; run syntax/local verification only unless owner supplies DB window. No new scheduler. | Honcho writes since 06-06 and the rich orphan corpus are single-location until dumped. |
| 19 | Run one restore/control efficacy drill: newest MVMS dump to scratch DB, `kanban.db` copy opens, LAN firewall probe. | josep-gated | Owner-supervised drill; no recurring schedule. Record row-count spot checks, sqlite open, and LAN probe result. | Write-only backups and untested firewall rules are comfort theater. |
| 20 | Reapply/verify remaining P1 security/control-plane partials from merge sweep, but do not blind cherry-pick. | LANE | Packet: inspect the P1 PARTIAL set affecting sandbox/worktree isolation, dispatch guardrails, provider runtime, peer-review/merge broker, and Codex null-output fallback; write a per-commit verdict with focused tests or a minimal port. Start with write-boundary and provider-runtime items; skip UI/generated obsolete items. | The sweep found one P1 clobber already fixed, but 41 partials remain; the security-relevant subset needs semantic confirmation. |
| 21 | Expand tests/fix edge behavior for Codex GC PR protection in installed/non-repo layouts. | LANE | Packet: resolve repo root via `git rev-parse --show-toplevel` or configured Hermes source path; test source-tree success and non-repo fallback; do not make GC destructive. | Current fix is safe but can silently lose open-PR branch defense outside source layout. |
| 22 | Clean local route/session clutter only after pin cleanup: stale Discord threads, junk/noise session pins, and profile route pins. | CLAUDE | Judgment cleanup using existing reports; no fresh mining. Treat archive as reversible; avoid active control lanes and Coinbase/legal clock threads. | Reduces restart/resume noise without hiding active work. |
| 23 | Journald retention: `SystemMaxUse=1G`, `MaxRetentionSec=14day`. | josep-gated | Root edit on owner terminal; verify post-reboot logs survive long enough to inspect failed backup/cron windows. | Post-mortems are currently impossible after a reboot; billing/security failures can disappear in hours. |
| 24 | Decide and document whether to disable the Daily X optimizer / x_search after revoked XAI token. | josep-gated | Either re-auth X from a real terminal or disable the cron/drop x_search until missed; no retry loop. | A revoked token creates daily useless auth churn and can mask real provider/auth failures. |
| 25 | Keep the 5 security/billing foundation cards and kill the two verifier-rejected card ghosts. | CLAUDE | Board judgment only after the six grilling answers: keep `t_2d7cf43c`, `t_ba8e769c`, `t_a9a31a1a`, `t_cb2e400e`, `t_ec024149`; do not resurrect `t_dcd7dfe5` or `t_c579fb7d`. | Prevents the board collapse from deleting real security work or preserving already-done/regressive cards. |

## (C) Top-3 risks if nothing further happens

1. **Billing bleed moves from theory to a clock.** The uncapped/shared OpenRouter key plus broken fallback block and remaining native Anthropic/profile pins leave a real spend path; C1 self-upgrades on June-15 if bare `claude -p` consumers remain.
2. **Security exposure stays one hop from full memory/control-plane compromise.** Default Supabase creds/demo JWT, LAN-open observability ports, wide gateway command rails, global `mvms-writer` inheritance, and stale profile credential symlinks are all simple delete/scope/rotate fixes — leaving them open is negligence, not complexity.
3. **Evidence and recovery keep failing exactly when needed.** Secret-bearing snapshots remain until shredded, app-state/Honcho backups are incomplete, restore drills have never run, journald forgets failures after a few hours, and stale Discord/session pins can resurrect old work on restart.

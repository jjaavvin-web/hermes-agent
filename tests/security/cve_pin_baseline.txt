# CVE-annotated exact pins that must NEVER be dropped/loosened by an upstream merge
# (audit SEC-2 supply-chain shrink-guard). One exact pin per line. A legitimate
# pin bump via `uv lock` must update this baseline IN THE SAME COMMIT.
requests==2.33.0
anthropic==0.87.0
aiohttp==3.13.4
starlette==1.3.1
